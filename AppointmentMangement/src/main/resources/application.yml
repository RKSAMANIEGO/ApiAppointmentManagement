spring:
  application:
    name: projectFinalAppointmentManagement
  datasource:
    url: jdbc:mysql://localhost:3306/bd_management_appointment
    username: root
    password: root
  jpa:
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration